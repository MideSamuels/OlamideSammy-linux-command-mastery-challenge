Day 2 Drill: File Operations

Objective

Create a nested folder structure, create three empty files, copy the folder to a backup location, rename one file, and safely remove the empty directories left behind.

Steps Completed

1. Created the nested directory structure `practice/2026/april`.
2. Created three empty files inside the `april` directory.
3. Copied the `practice` directory to a `backup` location.
4. Renamed one of the files.
5. Removed the files to make the directories empty.
6. Safely removed the empty directories from the deepest level upward.

