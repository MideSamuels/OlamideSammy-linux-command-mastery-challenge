pwd : shows where you currently are.
cd /var/log : moves to /var/log using an absolute path.
ls -lh : lists the contents in long, human-readable format.
cd ~ : takes you back to your home directory.
cd - : takes you back to the previous directory, which is /var/log.
ls -lh : confirms you're back in /var/log and displays its contents.
